high:
    учитывать шансы попасться с сокомандником

must have:
    сетка
        32 +
        16 +
        8 +
    раскидать const в settings и const +
    добавить картинку +
    icons instead of titles in cards +
    проверка на длину строки +
    ник обязательный, город и команда - нет +
    validate same nickname +
    validate trying to remove non existing participant +
    кнопка "create new" +
    кнопка "open event" +
    ui пишет в файл для каждого таба +
    удалять старую версию фестиваля +
    1 таб - одна сетка/один файл по названию категории и сегодняшней дате +
    Added button to rename event. +
    Should open frame with input and button to accept new event name +
    все инпуты capitalize сделать +
    в after_click_creators/update_category_data менять название таба в tab_control +
    validate remove tab button for tab existing +
    validate same event name in rename input +
    create_new_tab use in main window only. when category edition - we edit, not create! +
    BUT when edit category title - then all frames/widgets have to be renamed! +
    валидация на длину названия ивента +
    event name add bonus width cause of event_name cut after rename event +
    save in file empty categories also +
    dump json after category removing +
    add bonus width for grid size in category info +
    Tab Info block:
        Сделать все рабочим и для type == group +
        Вывод общего количества участников в категории (male/female) +
        edit category (grid size, title, type) +
            если уже есть участники - то нельзя поменять тип категории +
    move right separator to the right +
    if selected type is crew - remove '4 angles' from grid size +
    валидация на кол-во участников категории в зависимости от сетки +


    + randomizer
    список на 4 угла

    - много табов вылазят за пределы экрана если их много

    - редактировать инфо участника
    - unregister button with input of user number

    - after clicked 'create file' frame with load folder and button to change it
    - after clicked 'create' - messagebox: "Open file folder?" -> Yes, No

    - button to add event logo

    - Welcome grid image generator
    - Create tab only with first category

    drag'n'drop for tabs?

    navigation panel:
        file: +
            open; +
            create file; +
            create event +

        BattleGrid:
            settings:
                ! чекбокс открывать последний проект или всегда новый
                    (кеш на последние открытые табы)
                choose folder
        If event:
            Event:
                add_category
                !remove_category
                rename
